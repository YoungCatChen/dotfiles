# vim: set ft=zsh:
# Runs for every zsh, including the non-interactive shells that editors and AI
# coding agents spawn. Must be very lightweight and fast.

{{ include ".chezmoitemplates/nvm_dir.sh" }}
